import 'package:flutter/material.dart';

class Lipsticks extends StatelessWidget {
  const Lipsticks({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Lipsticks Art",
        style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold),
      ),
    );
  }
}
