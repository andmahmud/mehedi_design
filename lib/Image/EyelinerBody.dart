import 'package:flutter/material.dart';

class EyelinerBody extends StatelessWidget {
  const EyelinerBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Eyeliner Looks",
        style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold),
      ),
    );
  }
}
